version: '3.3'
services:
  swarm-manager:
    image: docker:dind
    container_name: swarm-manager
    privileged: true
    networks:
      - swarm

  swarm-worker:
    image: docker:dind
    container_name: swarm-worker
    privileged: true
    networks:
      - swarm
networks:
  swarm:
